# Linux/Colab mirror of requirements-smoke.lock's measured stack.
#
# The lock is Windows-pinned (win_amd64 wheels, triton-windows). This file pins
# the same versions for Linux so a Colab run records the same library stack as
# the local measurements. Two things are deliberate:
#
# * torch comes from PyTorch's cu128 index, not PyPI. PyPI's torch 2.11.0 for
#   Linux is the CUDA 13 build (its nvidia-* dependencies are *-cu13), which
#   needs a newer driver than Colab runtimes carry. The lock measured
#   torch-2.11.0+cu128, and that is what this installs.
# * unsloth 2026.8.18 declares trl<=0.24.0 and transformers<=5.5.0, so TRL 1.x
#   cannot share the environment (see requirements-smoke.in).
#
# Install with
#
#   pip install -r requirements-colab.txt && pip install --no-deps -e .
#
# and then check `torch.version.cuda` starts with "12." before measuring.
--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.org/simple
torch==2.11.0+cu128
torchvision==0.26.0+cu128
xformers==0.0.35
triton==3.6.0
unsloth==2026.8.18
unsloth-zoo==2026.8.12
transformers==5.5.0
trl==0.24.0
accelerate==1.14.0
bitsandbytes==0.50.1
huggingface-hub==1.27.0
peft==0.20.0
datasets==4.3.0
tokenizers==0.22.2
safetensors==0.8.0
sentencepiece==0.2.2
protobuf==7.35.1
cut-cross-entropy==25.1.1
msgspec==0.21.1
tyro==1.0.15
pyyaml==6.0.3
numpy==2.5.2
pydantic==2.13.4
