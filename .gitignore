.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.uv-cache-smoke/
.hf-cache-smoke/
.triton-cache-smoke/
unsloth_compiled_cache/
.coverage
htmlcov/
.env
.env.*
!.env.example
artifacts/
checkpoints/
wandb/
data/raw/
data/processed/
# Generated trajectories and built SFT rows are bulk. The committed record is
# configs/splits/sft_phase_a.json plus the hashed summary in results/.
data/teacher/
data/sft/
results/**/*.jsonl
results/**/*.parquet
notebooks/.ipynb_checkpoints/

# Working documents. These stay on the author's machine on purpose: they are
# planning and protocol material, not the result, and 35,000 words of it in
# front of the README buried the findings. DECISIONS.md is deliberately NOT
# here - the release gate in scripts/smoke_models.py reads it, so removing it
# turns CI red.
BLUEPRINT_v2.md
# Removed from the repository on request. The licence gate reads
# configs/release_decision.md instead, which carries the one entry it
# verifies. Track this again when the full log should be public.
DECISIONS.md
PLAN.md
HYPOTHESIS_PROTOCOL.md
RUNG_PROTOCOL.md
MODEL_SMOKE_PROTOCOL.md
SELF_CORRECTION_SPEC.md
RELATED_WORK.md
